LLM_PROVIDER=ollama
OLLAMA_URL=http://127.0.0.1:11434
OLLAMA_MODEL=gemma4:26b
OPENROUTER_API_KEY=
OPENROUTER_MODEL=openai/gpt-4o-mini
ALLOW_LOCAL_QUERY_FALLBACK=false
GITHUB_TOKEN=
# Optional. Omit this for the bundled backend/sift.db. Relative SQLite URLs
# are resolved from backend/ so production does not accidentally create an
# empty database from a different working directory.
# DATABASE_URL=sqlite:///./sift.db
